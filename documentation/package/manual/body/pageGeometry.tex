\usepackage[dvips]{geometry}

\newlength{\paperWidth}
\setlength{\paperWidth}{4.125in}
\newlength{\paperHeight}
\setlength{\paperHeight}{4.125in}

% trim a bit more to reduce bottom whitespace
%\setlength{\paperHeight}{0.987\paperHeight}

% proper book aspect ratio, but centered on page (for design doc presentation)
% manual overrides this
\geometry{papersize={\paperWidth,\paperHeight},scale=1.0, top=0.25in, bottom=0.375in, left=0.3125in, right=0.3125in, headheight=0in, headsep=0in, footskip=0.25in}

